services:
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"

  ai_worker:
    build: ./worker
    depends_on:
      - redis
    environment:
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=redis://redis:6379/0
    volumes:
      - ./worker:/app